# NOW -- The viewport spec follows the two-row phone rail (2026-09-16)

## The portrait-phone rail of t27.ai/#/queen is a two-row grid since gHashTag/trinity#1032; `specs/ui/viewport.t27` says so where it described the strip (Refs #3557)

- `specs/ui/viewport.t27` measured the Queen rail at 390x844 on 2026-09-09 as "a horizontal strip, 12 tiles of 44x74, about 6 visible" and pinned RAIL_* to that reading, "described, not changed here". The owner's phone showed the cost of the strip on 2026-09-16: six and a half tiles in view, the rest past the edge with no sign the row could be dragged. gHashTag/trinity#1032 lays the rail out as a grid of two rows on portrait phones (max-width 640px, `queen-phone.css`), ceil(views / 2) columns, every tile at least 44 px and in view, nothing scrolling; the head's tool row wraps instead of scrolling.
- The spec's SOURCE section now carries that dated note next to the measurement it supersedes, and the rail-capacity section says what its numbers are: the c881e7f reading, with thirteen views on the rail since TRI joined it (HUD_KEYS[12] = r) and the compact strip now the layout from 641 to 900 px wide only. No constant, test or assert changes; the site's generated `viewport.generated.ts` and `.css` follow when the copy is re-vendored (their spec hash changes, their values do not).
- Measured on the branch of trinity#1032 in headless Chrome at 360x740, 375x812 and 390x844: `.queen27-hud-command.is-compact` scrollWidth == clientWidth, 13 of 13 tiles inside the rail; the viewport contract's 390x844 kanban, map, factory and research combinations, failing on trinity main with `UNDECLARED SCROLLER .queen27-hud-vp-tools`, pass there.
- Not measured: a physical phone; only Chrome's device emulation. Not changed: RAIL_TILES stays 12 (the pinned reading), RAIL_MIN_VISIBLE stays 6 (met with every tile on a portrait phone).
